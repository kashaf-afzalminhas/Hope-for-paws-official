# samconfig.toml — fill Account/region on first `sam deploy --guided`
version = 0.1

[default.deploy.parameters]
stack_name = "hopeforpaws-api"
resolve_s3 = true
capabilities = "CAPABILITY_IAM"
confirm_changeset = true
# region = "us-east-1"
# parameter_overrides = "StageName=prod MongoUri=... JwtSecret=..."
